# ZeroPM 문서 정합성 점검 보고서

> **점검일**: 2026-06-12
> **대상**: `docs/` 디렉토리 (특히 `docs/01-유저스토리/`, `docs/01-설계/`)
> **방법**: max-effort 교차 검증 (9개 관점) — 내부 모순 / 삭제·이동 감사 / 교차 참조 / 기술 정확성 / AC 추적성 / 중복 / 구조 / 보안·운영 / 설계 깊이
> **계기**: `docs/` 디렉토리 재구성(폴더 이동·통합) 후 정합성 확인

---

## 1. 점검 배경

문서 재구성으로 다음 이동이 발생했고, 그에 따른 stale 참조·상태 모순·이슈 ID 충돌을 점검했다.

| 구 경로 | 신 경로 |
|---------|---------|
| `docs/05-PM-UX설계/` | `docs/01-유저스토리/` |
| `docs/06-아키텍처설계/` (본 설계 9개) | `docs/01-설계/` |
| `docs/06-아키텍처설계/` (참고 아카이브 4개) | `docs/01-설계/_참고/` |
| `docs/01-기능정의/` | `docs/_참고/01-기능정의/` |
| `docs/02-데이터설계/` | `docs/_참고/02-데이터설계/` |
| `docs/03-구현/` | `docs/_참고/03-구현/` |
| `docs/04-설계리뷰/` | `docs/_참고/04-설계리뷰/` |

---

## 2. 발견 항목 (심각도 순, 총 15건)

### 🔴 치명적 — 추적/계획을 직접 오도

| # | 항목 | 위치 | 상태 |
|---|------|------|------|
| 1 | **이슈 ID(H-/M-) 체계 충돌**: `03-시스템-아키텍처.md`(§9)가 `09-로드맵`/`00-설계-개요`와 다른 의미로 같은 ID 사용. H-4=Sentry vs 인덱스, H-5=console.log vs RBAC, M-4=CI vs save_cpm_results, M-5/M-6/H-2 등 충돌 | `03-시스템-아키텍처.md` §9 전체 | ⏳ 결정 필요 |
| 2 | **Phase 번호 체계 이중화**: 4-Phase 사용자가치 체계(`00-개요/02`·`02-기능`·`AC`) vs 6-Phase 기술계층 체계(`09-로드맵`). 같은 기능의 Phase가 상충 (예: Baseline=Phase 2 vs Phase 4, 자원=Phase 4 vs 5, 보고=Phase 3 vs 6) | `09-로드맵` §1~2 vs 나머지 | ⏳ 결정 필요 |
| 3 | **CPM "구현 완료" 모순**: `09-로드맵`이 CPM(Forward/Backward Pass)을 "구현 완료"로 표기했으나 `04-스케줄링`·`02-기능`·`00-설계-개요`는 모두 "미구현/계산 로직 없음" | `09-로드맵` line 16·68 | ✅ 수정됨 |
| 4 | **resources/assignments/costs 상태 3중 모순**: `05-DB`="구현 완료(Phase 2)" vs `02-기능`·`08`·`00-개요`="미구현(Phase 4/5)" | `05-DB` §2.1 | ✅ 수정됨 (스키마만 존재로 정정) |
| 5 | **DAG Graphlib·순환감지 "구현 완료" 모순**: `09-로드맵`="완료" vs `02-기능`(WBS-09·SCH-11)·`04-스케줄링`="미구현" | `09-로드맵` line 64·66 | ✅ 수정됨 |

### 🟠 정합성/내용 오류

| # | 항목 | 위치 | 상태 |
|---|------|------|------|
| 6 | **RBAC 역할 체계 불일치**: `admin/member/observer`(02-기능 AUTH-04) vs `owner/editor/viewer`(도처). 설계 문서가 스스로 지적한 H-5(enum 불일치)를 본문에서 재생산 | `02-기능` AUTH-04 | ✅ 수정됨 (owner/editor/viewer 통일) |
| 7 | **`00-설계-개요` 자기모순**: 보안 헤더가 line 68 "M-2", line 118 "M-8" 두 ID | `00-설계-개요` | ✅ 수정됨 (M-8) |
| 8 | **`00-설계-개요`가 최대 문서를 "미작성"으로 표기**: `01-사용자-UX설계.md`(146KB/2835줄)를 "미작성, docs/05-PM-UX설계/ 참조"로 기재 | `00-설계-개요` line 33 | ✅ 수정됨 |
| 9 | **NFR↔AC 성능 수치 충돌**: 재계산 NFR ≤500ms vs AC-03 "3초 이내"; 실시간 동기화 NFR ≤1s vs AC-07 "2초/5초" | `00-개요/02` vs `04-수용기준-AC` | ⏳ 결정 필요 |
| 10 | **거대 중복+stale 문서**: `01-사용자-UX설계.md`(146KB)가 `00-개요/01·02·03`을 통째 복제. 게다가 Flow 1~9만 포함(원본 `00-개요/03`은 1~11) → 뒤처진 복사본 | `01-설계/01-사용자-UX설계.md` | ⏳ 결정 필요 |

### 🟡 참조/수치 오류 (대부분 수정 완료)

| # | 항목 | 위치 | 상태 |
|---|------|------|------|
| 11 | `00-설계-개요` stale 경로: "이 폴더: docs/06-아키텍처설계/", "참고 아카이브(같은 폴더)", §6 폴더맵 전부 | `00-설계-개요` | ✅ 수정됨 |
| 12 | `00-README` stale: "User Flow 6개"(실제 11), "핵심 기능 10개"(실제 21), "../04-설계리뷰/"(이동) | `00-개요/00-README` | ✅ 수정됨 |
| 13 | 활성 cross-reference 깨짐: `02-기능` UI-09, `09-로드맵`, `03·04` 참고문서가 구 경로(`06-아키텍처설계/` 등) 가리킴 | 다수 | ✅ 수정됨 |
| 14 | 인덱스 개수 불일치: `05-DB`="7개"(내부 일관) vs `09-로드맵` Week1="6개". **목록 자체도 상이** (09는 tasks(status) 포함, DB는 project_sort·source_project 포함) | `05-DB` §4 vs `09-로드맵` line 141 | ⏳ 잔여 (인덱스 셋 정합 필요) |
| 15 | 함수 시그니처 불일치: `applyConstraint`·`createRecalculateFunction`·`calculateSuccessorDate`가 03·04 문서에서 제각각 | `03·04` | ✅ 수정됨 (실제 소스 기준 통일) |

> #15 비고: `15`는 당초 "Undo 20단계 vs 50회"를 후보로 보았으나, "최소 20단계 요구"와 "최대 50 캡"은 양립 가능(모순 아님)하여 제외하고, 함수 시그니처 불일치로 대체했다.

---

## 3. 이번 점검에서 수정한 내용 (2026-06-12)

| 파일 | 수정 |
|------|------|
| `00-설계-개요.md` | 폴더 경로(이 폴더/참고 아카이브/§6) 정정, `01-사용자-UX설계.md` 상태 정정, 보안헤더 M-2→M-8 |
| `00-개요/00-README.md` | Flow 6→11, AC 10→21, `../04-설계리뷰/`→`../_참고/04-설계리뷰/` |
| `09-운영-구현로드맵.md` | Phase 2/CPM/DAG/순환감지/업무캘린더 상태 정정, 참고·상세계획·자원 경로 3건 |
| `05-DB-스키마.md` | baseline_tasks M-4→L-6, resources/assignments/costs "스키마만 존재"로 정정, 참고 경로 |
| `02-기능-요구사항.md` | AUTH-04 역할 owner/editor/viewer 통일(H-5), stale 경로 4건 |
| `03-시스템-아키텍처.md` | 참고문서 경로 4건, 함수 시그니처(현재 구현 의사코드) 실제 소스 일치 |
| `04-스케줄링-엔진.md` | 참고 경로, 함수 시그니처(`applyConstraint`/`createRecalculateFunction`/`calculateSuccessorDate`) 실제 소스 일치 |

### 실제 소스 기준 정본 시그니처 (참고)
- `calculateSuccessorDate(predecessor, successor, linkType, lag = 0): Date | null`
- `calculateEndDate(startDate, duration): Date`
- `applyConstraint(task, calculatedStart, calculatedEnd): { start, end }`
- `createRecalculateFunction({ tasksRef, linksRef, handleTaskUpdate, toISOString })` → `recalculateAffectedTasks(changedTaskId, updatedTaskData = null, visited)`

---

## 4. 잔여 과제 (결정/후속 작업 필요)

| # | 항목 | 제안 |
|---|------|------|
| 1 | **이슈 ID 체계 통합** | `09-로드맵` §9를 마스터 레지스트리로 확정하고 `03-시스템-아키텍처.md`의 H-/M- ID를 그에 맞춰 재번호. 단 arch의 H-2(cascade bulk upsert)는 마스터에 대응 ID가 없어 신규 ID 부여 필요 |
| 2 | **Phase 체계 단일화** | 4-Phase 사용자가치 체계를 제품 "Phase"로 확정하고, `09-로드맵`의 6-Phase 기술계층은 "구현 계층(Layer)"으로 개명해 충돌 제거 (또는 그 반대). 기능별 Phase 태그 전수 재정렬 동반 |
| 3 | **거대 중복 문서 처리** | `01-설계/01-사용자-UX설계.md`(146KB)를 `00-개요/`로의 포인터(요약+링크)로 축약. 단, `00-개요/03`이 더 최신(Flow 11개)이므로 `00-개요/`를 SSOT로 |
| 4 | **NFR↔AC 수치 정합** | 재계산/동기화 성능을 NFR(엔지니어링 목표) vs AC(사용자 합격 상한)로 명시 구분하거나 단일 값으로 통일 |
| 5 | **인덱스 셋 정합** | `09-로드맵` Week1의 6개 인덱스 목록과 `05-DB`의 7개(`add_critical_indexes`) 목록을 일치 (tasks(status) vs project_sort/source_project 차이 해소) |
| 6 | **05-DB 본문 잔여** | §3.7/§3.9 본문에 남은 구식 `M-4`·"구현 완료(Phase 2)" 표기 정정 (이번엔 §2.1 테이블만 수정) |

---

## 5. 스코프 한계

- 정독 대상: `docs/01-유저스토리/*`, `docs/01-설계/00·02·03·04·05(앞부분)·09`.
- 깊이 검토하지 못한 문서: `06-보안-인증.md`, `07-실시간-협업.md`, `08-자원-비용-보고.md`, `05-DB-스키마.md`의 §3.2 이후 상세 스키마. 추가 점검 권장.

---

## 6. 후속 조치 — `docs/01-설계/` 전면 재작성 (2026-06-12)

§4 잔여 과제(전략적 결정 항목)는 `docs/01-설계/` 10개 문서 전면 재작성(v3.0)으로 모두 해소되었다. `docs/01-유저스토리/`(SSOT)·`docs/01-설계/_참고/`(원본 상세)·`docs/_참고/`(이전 설계)를 통합 근거로 사용했다.

| 잔여 과제 | 해소 결과 |
|-----------|-----------|
| 이슈 ID 통합 | `09-운영-구현로드맵.md §기술부채`를 마스터 레지스트리로 확정(H-1~8/M-1~13/L-1~13). `03`의 구 로컬 ID 전면 재매핑. M-13(cascade bulk upsert) 신규 등재 |
| Phase 체계 단일화 | 제품 4단계 = "Phase", 기술 6단계 = "구현 계층/Layer"로 용어 분리. `00-설계-개요 §4.2`에 Layer↔Phase 매핑 |
| 거대 중복 문서 | `01-사용자-UX설계.md`를 `docs/01-유저스토리/`(SSOT) 포인터+요약(약 130줄)으로 축약 |
| NFR↔AC 수치 | `docs/01-유저스토리/`를 AC/NFR SSOT로 확정, `01-설계`는 참조만 |
| 인덱스 셋 정합 | `05-DB-스키마`를 인덱스 정본으로 확정(핵심 7개). `09`·`00`이 이를 참조하도록 통일 |
| 함수 시그니처 | 실제 소스 기준 정본으로 `03`·`04` 통일 |
| 마이그레이션 파일명 | `05-DB-스키마` 기준(`20260612000001~000006`)으로 `09`·`00` 통일 |

정합성 검증 패스(10개 문서 교차 검증) 결과 이슈 ID·Phase/Layer·RBAC·시그니처·인덱스·헤더·경로 전 항목 정합 확인.
